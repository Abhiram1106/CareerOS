# Error Memory Entry — CareerOS Campus AI

<!-- Append one block like this to 03-ERRORS/error-memory.md per bug fixed. -->
<!-- Never overwrite existing entries — always append. -->

---

## YYYY-MM-DD — {Short error title}

| Field | Value |
|---|---|
| Date | YYYY-MM-DD |
| Status | resolved \| active \| recurring |
| Area | core-api \| ats-engine \| ai-rewriter \| resume-parser \| match-engine \| apps-web \| alembic \| celery \| docker \| scoring |
| Session | `01-SESSIONS/YYYY-MM-DD/session-HHMM-<tool>.md` |

**Symptom**: (what the user/system observed — exact error message if available)

**Root cause**: (why it happened — be precise enough that a future reader can identify the same class of error)

**Fix**: (exactly what was changed to resolve it — file paths + change description)

**Prevention rule**: (one imperative sentence — what to always/never do to avoid this)

**Do not repeat**: (the most important single thing to remember)

**Regression test added**: yes — `path/to/test` | no — reason | N/A
