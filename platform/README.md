# platform/

Repo-level engineering system.

| Folder | Contents |
|---|---|
| `omnix/` | Omnix Runtime committed configuration — agents, workflows, commands, settings. Runtime cache (`.omnix/memory/`) stays at repo root and is gitignored. |
