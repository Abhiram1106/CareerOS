export function AppFooter() {
  return (
    <footer className="app-footer">
      <p>
        CareerOS Campus AI — placement-readiness for Indian colleges. Scoring formula lives in{" "}
        <code>packages/scoring/</code> only.
      </p>
      <p className="muted">Officer dashboard · Intel bench · DPDP audit — on the roadmap.</p>
    </footer>
  );
}
